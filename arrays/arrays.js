// SYNTAX
{
    var numbers = []; // first syntax
    var numbers2 = []; // second type syntax
    var allusers = [];
    allusers.push({
        name: 'shubhat',
        email: 'a@b.com',
        age: 25
    });
    console.log(allusers);
}
